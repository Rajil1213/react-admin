/*
* Legacy Redux code no longer necessary, handled by RTK in `setUserReducer`

import { User } from "../../models/user";
export const setUser = (user: User) => {
    return {
        type: "SET_USER",
        user
    }
}
*/
// this required for compilation
export {}